import io.iyyel.celestialoutbreak.handler.SoundHandler;

public class SoundHandlerTestDriver {

    private static final SoundHandler soundHandler = SoundHandler.getInstance();

    public static void main(String[] args) throws InterruptedException {
/*
        System.out.println("Playing clip...");
        soundHandler.MENU_CLIP.play(true);

        for (int i = 10; i >= 0; i--) {
            System.out.println("Pausing in " + i + " seconds...");
            TimeUnit.SECONDS.sleep(1);
        }

        System.out.println("Pausing clip...");
        soundHandler.MENU_CLIP.pause();

        for (int i = 3; i >= 0; i--) {
            System.out.println("Resuming in " + i + " seconds...");
            TimeUnit.SECONDS.sleep(1);
        }

        System.out.println("Resuming clip...");
        soundHandler.MENU_CLIP.resume(true);

        for (int i = 5; i >= 0; i--) {
            System.out.println("Stopping in " + i + " seconds...");
            TimeUnit.SECONDS.sleep(1);
        }

        System.out.println("Stopping clip...");
        soundHandler.MENU_CLIP.stop();

        for (int i = 5; i >= 0; i--) {
            System.out.println("Terminating in " + i + " seconds...");
            TimeUnit.SECONDS.sleep(1);
        }
        */
    }

}